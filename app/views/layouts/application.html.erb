<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Smart List" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Smart List" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => 'reload' %>
    <%= csrf_meta_tags %>
  </head>
  <body class= "fixed skin-purple-light">
 	 <div class ="wrapper">
        <%= render 'layouts/navigation' %>
		<%= render 'layouts/navigation/sidebar'%>
  	  	<div class="content-wrapper"> 
	  	   
	         <%= render 'layouts/messages' %>
            <%= yield %>
		  
	   	</div>
	</div> 
	
        <%= render 'layouts/navigation/footer' %>

    </body>
</html>
