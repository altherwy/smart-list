var delete_all_confirm, r, txt;

$(document).ajaxComplete(function(event, request) {

#delete_all_confirm = function() {};

r = confirm('Press a button!');

if (r === true) {
  <% puts "yeeeees" %>

} 
else {
  txt = 'You pressed Cancel!';
}
                                                  });
